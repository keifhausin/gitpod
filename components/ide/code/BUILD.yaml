packages:
  - name: docker
    type: docker
    srcs:
      - "startup.sh"
    argdeps:
      - imageRepoBase
    config:
      dockerfile: leeway.Dockerfile
      image:
        - ${imageRepoBase}/ide/code:${version}